# Dispatcher work items

This repository can use its own dispatcher contract for future development. Create
`WI-NNN-name.md` with `Status: Ready`, `## Goal` and `## Acceptance criteria` only
after the work is authorized. `new-work-item.ps1` creates this minimal format.
